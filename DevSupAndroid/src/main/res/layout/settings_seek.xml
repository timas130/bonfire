<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp">

    <com.sup.dev.android.views.views.ViewCircleImage
        android:id="@+id/vDevSupIcon"
        android:layout_width="38dp"
        android:layout_gravity="center"
        android:layout_height="38dp"
        android:layout_marginRight="16dp"
        android:src="@drawable/ic_clear_black_24dp"
        android:clickable="false"
        android:focusable="false"
        android:padding="4dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:layout_marginLeft="8dp">

        <TextView
            android:id="@+id/vDevSupTitle"
            style="@style/TextAppearance.AppCompat.Body1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Seek bar title"/>

        <TextView
            android:id="@+id/vDevSupSubtitle"
            style="@style/Base.TextAppearance.AppCompat.Caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Seek bar subtitle with large description of action. More and more text."
            android:visibility="gone"/>

        <SeekBar
            android:id="@+id/vDevSupSeekBar"
            android:focusable="true"
            style="@style/Base.Widget.AppCompat.SeekBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </LinearLayout>

</LinearLayout>