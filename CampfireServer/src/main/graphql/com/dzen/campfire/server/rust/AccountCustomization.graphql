query AccountCustomization($userId: Int!) {
    internalAccountCustomization(userId: $userId) {
        userId
        nicknameColor {
            int
        }
        activeBadge {
            id
            miniImage {
                i
            }
        }
    }
}
