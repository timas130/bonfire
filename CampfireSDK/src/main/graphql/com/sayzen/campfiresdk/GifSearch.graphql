query GifSearch($query: String!, $after: String) {
    searchGif(term: $query, after: $after) {
        edges {
            cursor
            node {
                ...AttachGifItem
            }
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
    }
}
